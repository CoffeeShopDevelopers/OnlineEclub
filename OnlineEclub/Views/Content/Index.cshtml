@model IEnumerable<OnlineEclub.Models.Content>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContentTitle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContentTitle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContentID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContentID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
